"""SkillSQL-RL: recursive skill-augmented RL for enterprise Text-to-SQL.

This package is the *inference + benchmarking* framework on Google ADK 2.0
(datasource-agnostic connectors, semantic catalog, pluggable models, verifier,
and a single benchmark entry point). GRPO weight training lives in a separate
vLLM/verl track (see docs/).
"""

__version__ = "0.1.0"
